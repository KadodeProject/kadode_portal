version: "3"
services:
  deno:
    build:
      context: .
      dockerfile: ./infra/dev/deno/Dockerfile
    volumes:
      - ./frontend:/frontend
    environment:
      TZ: Asia/Tokyo
    tty: true
    ports:
      - 8000:8000

  nginx:
    build: ./infra/dev/nginx
    ports:
      - "7002:80"
    depends_on:
      - "deno"
